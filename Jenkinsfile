pipeline {
    agent any
    
  stages {
    stage("Build") {
       steps {
          echo "Hello, Pipeline!"
       }
   }
   stage("Test") {
       steps {
          echo "Hello, Pipeline 2 !"
       }
   }
 }
}
